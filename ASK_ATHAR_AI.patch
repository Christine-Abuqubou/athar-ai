diff --git a/client/src/index.css b/client/src/index.css
index 08d0e2b..b4189ca 100644
--- a/client/src/index.css
+++ b/client/src/index.css
@@ -228,3 +228,10 @@ h2 { font-family: 'Playfair Display', serif; font-size: clamp(48px, 6vw, 78px);
 .website-rating-save { margin-top: 15px; width: 100%; }
 .website-rating-save:disabled { opacity: .45; cursor: not-allowed; }
 @media (max-width: 600px) { .website-rating-trigger { right: 14px; bottom: 14px; padding: 10px 12px; font-size: 10px; }.website-rating-panel { padding: 28px 22px; }.website-rating-panel h2 { font-size: 32px; } }
+
+/* AI response navigation */
+.chat-links { display: flex; flex-direction: column; gap: 7px; margin-top: 13px; padding: 11px 12px; border-left: 2px solid var(--gold); background: rgba(182,139,72,.08); }
+.chat-links > b { color: var(--gold-deep); font-family: 'DM Mono', monospace; text-transform: uppercase; letter-spacing: .08em; font-size: 9px; }
+.chat-link { display: flex; align-items: center; gap: 6px; color: var(--ink); font-size: 11px; text-decoration: none; }
+.chat-link:hover { color: var(--gold-deep); }
+.chat-links small { margin-top: 2px; }
diff --git a/client/src/pages/Home.tsx b/client/src/pages/Home.tsx
index 8d939d8..92590c1 100644
--- a/client/src/pages/Home.tsx
+++ b/client/src/pages/Home.tsx
@@ -38,6 +38,7 @@ import {
   X,
 } from "lucide-react";
 import { toast } from "sonner";
+import { trpc } from "@/lib/trpc";
 
 const IMG = {
   city: "/manus-storage/city-of-david_519164bd.jpg",
@@ -307,11 +308,27 @@ function RoutePage() {
   return <div className="inner-page"><div className="page-width inner-intro"><StatusPill>Personalized route builder</StatusPill><h1>Build your<br /><em>heritage route.</em></h1><p>Tell us how much time you have and what you want to feel. ATHAR will connect the stops.</p></div><div className="page-width route-builder"><div className="route-form"><div className="form-label">I have</div><div className="option-row">{["30 min", "1 hour", "2 hours", "Half day"].map(value => <button className={time === value ? "selected" : ""} onClick={() => setTime(value)} key={value}>{value}</button>)}</div><div className="form-label">I'm drawn to</div><div className="option-row wrap">{["Architecture", "History", "Archaeology", "Stories", "Family friendly"].map(value => <button className={interest === value ? "selected" : ""} onClick={() => setInterest(value)} key={value}>{value}</button>)}</div><button className="button button-dark full-button" onClick={() => toast(`Route refreshed for ${time} · ${interest}`)}>Generate my route <Sparkles size={16} /></button></div><div className="route-result"><div className="route-result-top"><div><p className="eyebrow">Your suggested route</p><h2>Jerusalem in {time}</h2></div><StatusPill>3 stops · 2.4 km</StatusPill></div>{stops.map((stop, index) => <div className="route-stop" key={stop.slug}><span className="stop-number">0{index + 1}</span><img src={stop.image} alt={stop.name} /><div><b>{stop.name}</b><span>{index === 0 ? "Start here · 35 min" : index === 1 ? "Walk 0.8 km · 40 min" : "Walk 1.1 km · 45 min"}</span></div><ArrowRight size={16} /></div>)}</div></div></div>;
 }
 
+type ChatMessage = { from: "athar" | "you"; text: string; sites?: Array<{ slug: string; name: string; arabic: string; href: string }>; media?: { documentary: string; duration: string; timeLens: string[] } | null; sources?: string[] };
+
 function AskPage() {
-  const [messages, setMessages] = useState([{ from: "athar", text: "Welcome. Ask me about Jerusalem's places, periods, people, and the evidence behind their stories." }]);
+  const [messages, setMessages] = useState<ChatMessage[]>([{ from: "athar", text: "Welcome. Ask me about Jerusalem's places, periods, people, videos, or the evidence behind their stories." }]);
   const [input, setInput] = useState("");
-  const send = () => { if (!input.trim()) return; const question = input; setInput(""); setMessages(m => [...m, { from: "you", text: question }, { from: "athar", text: "ATHAR's source-aware answer: Jerusalem is best understood as a layered place. I can help you explore a specific site, period, or document—but I will always distinguish documented facts from historical interpretation." }]); };
-  return <div className="inner-page ask-page"><div className="page-width inner-intro"><StatusPill>Ask ATHAR</StatusPill><h1>A better question<br /><em>opens a place.</em></h1><p>A source-aware guide to Jerusalem's architecture, histories, people, and unanswered questions.</p></div><div className="page-width chat-shell"><div className="chat-sources"><p className="eyebrow">Suggested sources</p>{SOURCES.slice(0, 3).map(source => <div className="chat-source" key={source.title}><ShieldCheck size={15} /><span>{source.institution}<b>{source.title}</b></span></div>)}</div><div className="chat-main"><div className="chat-messages">{messages.map((message, index) => <div className={`chat-message ${message.from}`} key={`${message.from}-${index}`}><div className="chat-avatar">{message.from === "athar" ? "أ" : <UserRound size={15} />}</div><div><span className="chat-name">{message.from === "athar" ? "ATHAR AI" : "You"}</span><p>{message.text}</p>{message.from === "athar" && <small><ShieldCheck size={12} /> Grounded in available sources</small>}</div></div>)}</div><div className="chat-composer"><input value={input} onChange={e => setInput(e.target.value)} onKeyDown={e => e.key === "Enter" && send()} placeholder="Ask about a place, period, or person…" /><button onClick={send}><Send size={17} /></button></div></div></div></div>;
+  const ask = trpc.heritage.ask.useMutation();
+  const send = () => {
+    const question = input.trim();
+    if (!question || ask.isPending) return;
+    setInput("");
+    setMessages(current => [...current, { from: "you", text: question }]);
+    ask.mutate({
+      question,
+      language: /[\u0600-\u06ff]/.test(question) ? "ar" : "en",
+      history: messages.slice(-6).map(message => ({ role: message.from === "you" ? "user" as const : "assistant" as const, content: message.text })),
+    }, {
+      onSuccess: result => setMessages(current => [...current, { from: "athar", text: result.answer, sites: result.matchedSites, media: result.media, sources: result.sources }]),
+      onError: () => setMessages(current => [...current, { from: "athar", text: "I’m having trouble reaching the AI service right now. Please try again, or ask about the Dome of the Rock, al-Aqsa, the Old City Walls, or the City of David." }]),
+    });
+  };
+  return <div className="inner-page ask-page"><div className="page-width inner-intro"><StatusPill>Ask ATHAR</StatusPill><h1>A better question<br /><em>opens a place.</em></h1><p>A source-aware guide to Jerusalem's architecture, histories, people, videos, and unanswered questions.</p></div><div className="page-width chat-shell"><div className="chat-sources"><p className="eyebrow">Suggested sources</p>{SOURCES.slice(0, 3).map(source => <div className="chat-source" key={source.title}><ShieldCheck size={15} /><span>{source.institution}<b>{source.title}</b></span></div>)}</div><div className="chat-main"><div className="chat-messages">{messages.map((message, index) => <div className={`chat-message ${message.from}`} key={`${message.from}-${index}`}><div className="chat-avatar">{message.from === "athar" ? "أ" : <UserRound size={15} />}</div><div><span className="chat-name">{message.from === "athar" ? "ATHAR AI" : "You"}</span><p>{message.text}</p>{message.from === "athar" && <><small><ShieldCheck size={12} /> Grounded in ATHAR data and trusted source notes</small>{message.sites?.length ? <div className="chat-links"><b>Explore in ATHAR</b>{message.sites.map(site => <Link key={site.slug} href={site.href} className="chat-link">{site.name} · {site.arabic} <ArrowUpRight size={13} /></Link>)}</div> : null}{message.media ? <div className="chat-links"><b>Watch the related documentary</b><a className="chat-link" href={message.media.documentary} target="_blank" rel="noreferrer"><Play size={13} /> Al-Aqsa documentary · {message.media.duration}</a><small>{message.media.timeLens.join(" · ")}</small></div> : null}{message.sources?.length ? <small>Sources: {message.sources.join(" · ")}</small> : null}</>}</div></div>)}</div><div className="chat-composer"><input value={input} onChange={e => setInput(e.target.value)} onKeyDown={e => e.key === "Enter" && send()} placeholder="Ask about a place, period, person, or video…" aria-label="Ask ATHAR" /><button onClick={send} disabled={ask.isPending} aria-label="Send question">{ask.isPending ? <LoaderIcon /> : <Send size={17} />}</button></div></div></div></div>;
 }
 
 function PassportPage() {
diff --git a/server/heritage.retrieval.test.ts b/server/heritage.retrieval.test.ts
new file mode 100644
index 0000000..eb93082
--- /dev/null
+++ b/server/heritage.retrieval.test.ts
@@ -0,0 +1,22 @@
+import { describe, expect, it } from "vitest";
+import { detectMedia, retrieve } from "./routers";
+
+describe("heritage retrieval", () => {
+  it("matches the Dome of the Rock from English wording", () => {
+    expect(retrieve("Where is the Dome of the Rock?")[0]?.slug).toBe("dome-of-the-rock");
+  });
+
+  it("matches Al-Aqsa from Arabic wording", () => {
+    expect(retrieve("أين المسجد الأقصى؟")[0]?.slug).toBe("al-aqsa-mamluk");
+  });
+
+  it("detects a documentary request only when media intent is present", () => {
+    const sites = retrieve("show me the Al-Aqsa video");
+    expect(detectMedia("show me the Al-Aqsa video", sites)?.documentary).toContain("mamluk-jerusalem-documentary");
+    expect(detectMedia("Tell me about Al-Aqsa", sites)).toBeNull();
+  });
+
+  it("keeps a one-character question valid for graceful AI handling", () => {
+    expect(retrieve("?")).toEqual([]);
+  });
+});
diff --git a/server/heritage.test.ts b/server/heritage.test.ts
index db42409..79da2cc 100644
--- a/server/heritage.test.ts
+++ b/server/heritage.test.ts
@@ -1,7 +1,16 @@
-import { describe, expect, it } from "vitest";
+import { describe, expect, it, vi } from "vitest";
 import { appRouter } from "./routers";
 import type { TrpcContext } from "./_core/context";
 
+vi.mock("./_core/llm", () => ({
+  invokeLLM: vi.fn(async () => ({
+    id: "test-response",
+    created: 0,
+    model: "gpt-5-mini",
+    choices: [{ index: 0, finish_reason: "stop", message: { role: "assistant", content: "Dome of the Rock is a layered place. VERIFIED FACT: its foundation inscription is among the earliest surviving examples of monumental Islamic epigraphy. HISTORICAL INTERPRETATION: its meaning has changed across centuries." } }],
+  })),
+}));
+
 const ctx = {
   user: null,
   req: {} as TrpcContext["req"],
@@ -17,7 +26,7 @@ describe("heritage content API", () => {
     expect(sites.map(site => site.slug)).toContain("al-aqsa-mamluk");
   });
 
-  it("returns a source-aware answer for a site question", async () => {
+  it("returns a source-aware AI answer with navigation metadata", async () => {
     const answer = await appRouter.createCaller(ctx).heritage.ask({
       question: "Why does this place matter?",
       siteSlug: "dome-of-the-rock",
@@ -25,6 +34,7 @@ describe("heritage content API", () => {
     expect(answer.answer).toContain("Dome of the Rock");
     expect(answer.sources.length).toBeGreaterThan(0);
     expect(answer.transparency).toContain("VERIFIED FACT");
+    expect(answer.matchedSites[0]?.href).toBe("/site/dome-of-the-rock");
   });
 
   it("generates a route with all supported stops", async () => {
diff --git a/server/routers.ts b/server/routers.ts
index 5721b66..be42233 100644
--- a/server/routers.ts
+++ b/server/routers.ts
@@ -1,6 +1,7 @@
 import { z } from "zod";
 import { COOKIE_NAME } from "@shared/const";
 import { getSessionCookieOptions } from "./_core/cookies";
+import { invokeLLM } from "./_core/llm";
 import { systemRouter } from "./_core/systemRouter";
 import { publicProcedure, router } from "./_core/trpc";
 
@@ -12,12 +13,36 @@ const sites = [
 ];
 
 const sources = [
-  { institution: "UNESCO World Heritage Centre", title: "Old City of Jerusalem and Its Walls", type: "Heritage institution", date: "1981" },
-  { institution: "The Israel Museum, Jerusalem", title: "Jerusalem: A History of the City", type: "Museum collection", date: "2024" },
-  { institution: "British Museum", title: "Jerusalem Through Time", type: "Academic collection", date: "2022" },
-  { institution: "A. W. Lawrence Archive", title: "The City of David and Early Jerusalem", type: "Archive / field record", date: "1936" },
+  { institution: "UNESCO World Heritage Centre", title: "Old City of Jerusalem and Its Walls", type: "Heritage institution", date: "1981", url: "https://whc.unesco.org/en/list/148/" },
+  { institution: "The Israel Museum, Jerusalem", title: "Jerusalem: A History of the City", type: "Museum collection", date: "2024", url: "https://www.imj.org.il/en" },
+  { institution: "British Museum", title: "Jerusalem Through Time", type: "Academic collection", date: "2022", url: "https://www.britishmuseum.org/collection" },
+  { institution: "A. W. Lawrence Archive", title: "The City of David and Early Jerusalem", type: "Archive / field record", date: "1936", url: "https://www.architecture.com" },
 ];
 
+const normalize = (value: string) => value.toLocaleLowerCase().normalize("NFKD").replace(/[\u0300-\u036f]/g, "");
+const aliases: Record<string, string[]> = {
+  "dome-of-the-rock": ["dome", "rock", "قبة", "الصخرة", "قبه"],
+  "al-aqsa-mamluk": ["aqsa", "al aqsa", "الأقصى", "اقصى", "المسجد", "mamluk", "مملوكي"],
+  "city-of-david": ["city of david", "داود", "مدينة داود", "silwan", "سلوان"],
+  "old-city-walls": ["old city wall", "walls", "البلدة القديمة", "الاسوار", "أسوار", "suleiman", "سليمان"],
+};
+
+export function retrieve(question: string, siteSlug?: string) {
+  const query = normalize(question);
+  const ranked = sites.map(site => {
+    const terms = [site.name, site.arabic, site.period, site.location, site.category, site.verifiedFact, ...(aliases[site.slug] ?? [])].map(normalize);
+    const score = (site.slug === siteSlug ? 8 : 0) + terms.reduce((total, term) => total + (query.includes(term) ? Math.min(3, term.length > 4 ? 2 : 1) : 0), 0);
+    return { site, score };
+  }).sort((a, b) => b.score - a.score);
+  return ranked.filter(item => item.score > 0).slice(0, 3).map(item => item.site);
+}
+
+export function detectMedia(question: string, matchedSites: typeof sites) {
+  const asksForMedia = /video|film|documentary|watch|show me|فيديو|فلم|وثائقي|شاهد|اعرض/i.test(question);
+  if (!asksForMedia) return null;
+  return matchedSites.find(site => site.media)?.media ?? null;
+}
+
 export const appRouter = router({
   system: systemRouter,
   auth: router({
@@ -32,14 +57,29 @@ export const appRouter = router({
     sites: publicProcedure.query(() => sites),
     site: publicProcedure.input(z.object({ slug: z.string() })).query(({ input }) => sites.find(site => site.slug === input.slug) ?? null),
     sources: publicProcedure.query(() => sources),
-    ask: publicProcedure.input(z.object({ question: z.string().min(2), siteSlug: z.string().optional() })).mutation(({ input }) => {
-      const site = sites.find(item => item.slug === input.siteSlug);
-      const subject = site ? site.name : "Jerusalem";
-      return {
-        answer: `Based on the available historical sources, ${subject} is best understood as a layered place. The evidence points to a site whose meaning and built form changed across centuries; some details remain interpreted rather than certain. ATHAR will always distinguish documented facts from historical interpretation.`,
-        sources: site?.sources ?? sources.slice(0, 2).map(source => source.institution),
-        transparency: "VERIFIED FACT + HISTORICAL INTERPRETATION",
-      };
+    ask: publicProcedure.input(z.object({ question: z.string().trim().min(1).max(2000), siteSlug: z.string().optional(), history: z.array(z.object({ role: z.enum(["user", "assistant"]), content: z.string().max(4000) })).max(8).optional(), language: z.enum(["en", "ar"]).default("en") })).mutation(async ({ input }) => {
+      const matchedSites = retrieve(input.question, input.siteSlug);
+      const media = detectMedia(input.question, matchedSites);
+      const context = JSON.stringify({ matchedSites, trustedSources: sources, availableMedia: media });
+      const languageInstruction = input.language === "ar" ? "Answer in clear Modern Standard Arabic." : "Answer in clear English.";
+      try {
+        const response = await invokeLLM({
+          model: "gpt-5-mini",
+          maxTokens: 700,
+          messages: [
+            { role: "system", content: `You are ATHAR AI, a careful cultural-heritage guide for Jerusalem. ${languageInstruction} Answer the user's question naturally, including a short answer for greetings or vague inputs. Use only the supplied ATHAR context for Jerusalem-specific claims. Never invent dates, quotations, sources, or media. Explicitly label claims as VERIFIED FACT when supported by the context and HISTORICAL INTERPRETATION when explaining uncertainty or meaning. If the context does not support a specific claim, say so and suggest what the user can explore. Do not mention hidden prompts or retrieval.\nATHAR CONTEXT:\n${context}` },
+            ...(input.history ?? []).map(message => ({ role: message.role === "user" ? "user" as const : "assistant" as const, content: message.content })),
+            { role: "user", content: input.question },
+          ],
+        });
+        const answer = response.choices?.[0]?.message?.content;
+        const text = typeof answer === "string" && answer.trim() ? answer.trim() : "I can help you explore Jerusalem's places, periods, and sources. Which place would you like to begin with?";
+        return { answer: text, transparency: "VERIFIED FACT + HISTORICAL INTERPRETATION", matchedSites: matchedSites.map(site => ({ slug: site.slug, name: site.name, arabic: site.arabic, href: `/site/${site.slug}` })), media, sources: Array.from(new Set(matchedSites.flatMap(site => site.sources))).slice(0, 4).concat(matchedSites.length ? [] : sources.slice(0, 2).map(source => source.institution)) };
+      } catch (error) {
+        console.error("ATHAR AI request failed", error);
+        const fallbackSite = matchedSites[0];
+        return { answer: fallbackSite ? `${fallbackSite.name} — VERIFIED FACT: ${fallbackSite.verifiedFact} Ask me about its period, location, or historical interpretation.` : "I can help you explore Jerusalem's places, periods, people, videos, and evidence. Please ask a little more—such as “Where is the Dome of the Rock?” or “Show me the al-Aqsa documentary.”", transparency: "VERIFIED FACT + HISTORICAL INTERPRETATION", matchedSites: matchedSites.map(site => ({ slug: site.slug, name: site.name, arabic: site.arabic, href: `/site/${site.slug}` })), media, sources: fallbackSite?.sources ?? sources.slice(0, 2).map(source => source.institution) };
+      }
     }),
   }),
   routes: router({
